# Reconstructed writing-operation result

The raw text MDL winner is the one-role independent-sign model: 52,412.893 bits. A one-role tick388-after component model follows at 52,413.180 bits. This comparison does not select a graphical interpretation. Among three-role models, tick388-after is strongest, but its additional role parameters do not win the overall MDL comparison. These are newly executed results, not the lost run's numbers.

Ordered additions enter the raw emission probability through a sum over preceding or following component roles. Every raw-sign distribution sums to one across the original 417 sign IDs. The model learns role emissions and transitions together, then normalizes competing raw signs; expanding a sign never creates another observation. The reported text costs condition on the observed sequence length.

The preserved roof87/tick211 exact-equivalence argument remains separate. Under that explicit conditional rewrite, object1380's 65–70–211 and object2452's 87–59–71 become 87–59–70–211. The raw predictive grammar does not establish that either rewrite preserves meaning. The picture contrast identified in Route B remains a constraint on any proposed literal species reading.

The stronger three-role alternative assigns boundary probability approximately0.001 between66 and336 in 387–66–336–89–211 (object1147). Thus this independently fitted categorical model does not support the specific local split previously proposed to repair whole-line211 exclusion. The recurring336–89–211 suffix remains real evidence in the recovered exact-expression analysis; its existence alone does not force a word boundary in this occurrence.

The worked latent analyses retain continuous role and boundary alternatives rather than forcing a complete reading. No boundary is named genitive, plural, word, or clause. Seven common frozen evaluations, with numerical-face tokens masked out of the text channel, belong to the coordinator's joint results. Six complete graphical-pairing searches control for selection across the same candidate family.

`run_contextual.py` executes metadata-conditioned unigram rivals on the same seven frozen raw-text target sets, alongside a freshly fitted one-role global baseline. Fixed shrinkage uses ten background pseudo-observations per context. Both site and object-class conditioning lose to the global model on every recombination fold and on the70/71 family. Object-class conditioning improves medium3 transfer by14.04 bits; site conditioning loses163.62 bits there. This is a result for the tested fixed-shrinkage categorical rivals, not rejection of every local grammatical convention. Full per-expression predictions and parameters are retained in contextual_evaluations.json. These raw-text comparisons are separate from the joint score, whose text channel removes count-face targets.
