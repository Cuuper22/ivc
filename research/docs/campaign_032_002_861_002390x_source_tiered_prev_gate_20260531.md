# 002-390-X source-tiered predecessor gate

Date: 2026-05-31 America/Los_Angeles

Status: active campaign gate, not goal completion.

## Run control

This gate starts from the replacement branch-sign ecology frame file:

- `data/open_prototype/reports/campaign_032_002_861_002390x_branch_sign_ecology_20260531_002390_frames.csv`

It does not use the quarantined post-cutoff `source_normalized_family_collapse` artifact as evidence. The question is whether the apparent `002-390-X` branch-tail pattern contains a source-controlled same-predecessor contrast, or whether the strict rows only show polarity across unmatched frames.

No value, phonetics, language identity, function, sign meaning, or translation is accepted.

## Outputs

- `data/open_prototype/tools/campaign_032_002_861_002390x_source_tiered_prev_gate_20260531.mjs`
- `data/open_prototype/reports/campaign_032_002_861_002390x_source_tiered_prev_gate_20260531_rows.csv`
- `data/open_prototype/reports/campaign_032_002_861_002390x_source_tiered_prev_gate_20260531_prev_summary.csv`
- `data/open_prototype/reports/campaign_032_002_861_002390x_source_tiered_prev_gate_20260531_decisions.csv`
- `data/open_prototype/reports/campaign_032_002_861_002390x_source_tiered_prev_gate_20260531_summary.json`

## Source tiers

The 15 local `002-390-X` frames were tiered as follows:

| Tier | Meaning in this gate |
|---|---|
| `strict_visible` | Existing checkpoint row-level source-visible witness. |
| `panel_bound_not_strict` | Source panel or boxed window is acquired, but token identities/boundaries are still catalog-mediated. |
| `public_panel_downweighted` | Public panel pressure exists but is explicitly downweighted. |
| `route_or_metadata_pressure` | Supplement, route, secondary icon, or metadata-cluster pressure without image binding. |
| `metadata_or_blocked` | Local metadata only, object-ID blocked, or unresolved source bridge. |

## Row matrix

| Object | Prev before `002` | Branch after `390` | Tail | Class | Source tier |
|---|---|---|---|---|---|
| H-1993 | `004` | `095` | `<END>` | terminal | route_or_metadata_pressure |
| H-68 | `003` | `072` | `<END>` | terminal | metadata_or_blocked |
| H-773 | `803` | `530` | `741` | continuing | panel_bound_not_strict |
| M-38 | `235` | `125` | `632 032` | continuing | metadata_or_blocked |
| M-70 | `032` | `692` | `<END>` | terminal | strict_visible |
| M-71 | `142` | `095` | `<END>` | terminal | strict_visible |
| M-119 | `484` | `125` | `632 032 900 563` | continuing | strict_visible |
| M-213 | `384` | `346` | `<END>` | terminal | metadata_or_blocked |
| M-232 | `236` | `707` | `<END>` | terminal | metadata_or_blocked |
| M-708 | `061` | `140` | `<END>` | terminal | metadata_or_blocked |
| M-735 | `235` | `125` | `195` | continuing | strict_visible |
| -:3335.1 | `032` | `590` | `032` | continuing | metadata_or_blocked |
| Sktd-1 | `004` | `125` | `820` | continuing | public_panel_downweighted |
| M-1825 | `031` | `705` | `<END>` | terminal | route_or_metadata_pressure |
| -:4237.1 | `388` | `705` | `<END>` | terminal | route_or_metadata_pressure |

## Strict core

The strict source-visible layer has four rows:

| Object | Frame | Class |
|---|---|---|
| M-70 | `032 -> 002-390 -> 692 -> <END>` | terminal |
| M-71 | `142 -> 002-390 -> 095 -> <END>` | terminal |
| M-119 | `484 -> 002-390 -> 125 -> 632 032 900 563` | continuing |
| M-735 | `235 -> 002-390 -> 125 -> 195` | continuing |

This preserves branch-tail polarity as pressure: strict `125` rows continue, and strict non-`125` rows close. But the strict rows do not form a same-predecessor split. Each strict predecessor group has only one branch alternative.

Decision: strict polarity is not the same thing as grammar promotion. The live claim remains a branch-tail ecology hypothesis, not an accepted constructional function.

## Matched predecessor groups

Only three predecessor groups have more than one local row:

| Prev before `002` | Local split | Gate result | Consequence |
|---|---|---|---|
| `004` | H-1993 `095 -> <END>` vs Sktd-1 `125 -> 820` | `matched_prev_split_non_strict_only` | H-1993 is route-only and Sktd-1 is downweighted, so this cannot be used as a source-controlled contrast yet. |
| `032` | M-70 `692 -> <END>` vs `3335.1` `590 -> 032` | `matched_prev_split_partly_strict_blocked` | M-70 is strict, but `3335.1` is object-ID blocked and formula-family pressured. |
| `235` | M-38 `125 -> 632 032` vs M-735 `125 -> 195` | `same_prev_same_branch_tail_split` | This tests tail subframes under branch `125`, not branch alternation. M-38 is still weak. |

H-773 does not rescue a matched split. It has predecessor `803`, branch `530`, tail `741`, and source tier `panel_bound_not_strict`; it is a singleton predecessor and remains non-strict after the side-B guard.

## Decision

Status: `strict_core_polarity_unmatched_prev_gate_blocks_grammar_no_values`.

What survives:

- `002-390-X` remains a live structural object.
- Strict row-level source pressure still supports a polarity question: `125` continuations versus non-`125` closures in the current strict core.
- The positive model can continue as a branch-tail ecology hypothesis.

What is blocked:

- No strict same-predecessor branch split exists.
- The `004` split is blocked until H-1993 image-binds and Sktd-1 becomes stricter.
- The `032` split is blocked until `3335.1` gets a real object/source bridge and survives `390-590-032` family collapse.
- The `235` pair is a tail split under the same `125` branch, not a branch-choice contrast.
- H-773 remains anti-lazy pressure only; it is not a strict matched anti-`125` exception.

Accepted value, phonetics, language identity, function, sign meaning, and translation remain 0.

## Follow-on Sktd-1 Addendum

The Sktd-1 half of the `004` split was rechecked in `docs/campaign_032_002_861_002390x_sktd1_side_pair_recheck_20260531.md`.

That recheck improves Sktd-1 from loose public-panel pressure to source-panel side-pair visibility: CISI India leaf `n397` visibly labels `Sktd-1 A` and `Sktd-1 a`. It also confirms why Sktd-1 still cannot be strict. The layout is wrapped, with a top inscription band plus a separate lower-field sign, so the exact local sequence `390-004-002-390-125-820` remains catalog-mediated.

The `004` matched-predecessor split therefore remains blocked:

- H-1993 is still route-only/no image.
- Sktd-1 is panel-bound side-pair visible, but not strict token evidence.

## Follow-on Near-Frame Scout

The damaged/open and gapped near-frame scout was recorded in `docs/campaign_032_002_861_002390x_near_frame_scout_20260531.md`.

Status: `near_frame_scout_no_hidden_matched_prev_rescue_no_values`.

The adjacent `002-390` set remains exactly 15 rows, with 0 damaged/open adjacent rows. The only gapped `002-X-390` rows are K-89 `002-595-390` and Dholavira `4348.1` `002-861-390`. Neither creates a same-predecessor branch alternative. The `4348.1` result also reinforces the ICIT 4348 guard: it is not a bridge into the `4237.1 / 002-390-705` lane.

## H-1993 Acquisition Packet

The missing source witness for the `004` split is now packaged in `docs/campaign_032_002_861_002390x_h1993_acquisition_packet_20260531.md`.

Status: `h1993_acquisition_packet_ready_no_image_binding`.

The packet names the target as `H-1993 / H96-2769 / ICIT 744`, local row `744.2`, local text `+740-000-220-004-002-390-095+`, and asks for source photograph, plate/page reference, side labels, source transcription, and damage/copy notes. It was not sent as email in this gate.

## Next gate

The next useful work is not another broad defense of `125`. The cleanest live routes are:

1. Acquire/bind H-1993 in full CISI 3.1 or equivalent archive/museum source, because it could sharpen the `004` split.
2. Acquire/bind Dholavira `4237.1` through `8758 / ZA-12:2` or an equivalent caption/image bridge, because repeated `705` is still source-gated.
3. Find a strict same-predecessor branch alternative inside `002-390-X`, or accept that the current evidence is polarity pressure across unmatched frames.
4. Keep testing formula/register alternatives before any function or grammar promotion.
