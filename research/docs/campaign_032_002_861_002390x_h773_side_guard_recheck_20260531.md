# H-773 side-guard recheck for `002-390-X`

Date: 2026-05-31 America/Los_Angeles

Status: active source/sign gate, not goal completion.

## Target

H-773 is the in-frame continuing non-`125` pressure row:

- Target row: `1665.1`
- Target side: local `H-773 A`
- Local text: `+740-798-803-002-390-530-741+`
- Structural stressor: `002-390-530-741`, where non-`125` branch `530` continues to `741`
- Local excavation/source pointer: `12377351`

The previous H-773 recheck already made H-773 source-panel-bound through CISI Pakistan page `n358`, but not token-strict.

## New side route

Local Kenoyer/Meadow 1997 text has a route that initially looks like an H-773 upgrade:

`p.57:5 | XCIV, 351 | 12377 | F Trench IV | Square I 14/22 | H-773B | One side only of the above eight H94/H95 tablets`

This is useful, but not for the target side.

It likely explains the local source pointer `12377351` as Vats object `12377` plus Plate XCIV item `351`. The already downloaded Vats Plate XCIV image visibly contains item `351`; a crop is stored at:

`tmp/002390x_h773_vats_side_guard_20260531/Vats_Plate_XCIV_351_candidate_H773B_tight_from_v2.png`

## Side guard

The route names `H-773B`, not `H-773A`.

Local H-773 side rows are:

| Local row | Side | Local text | Role |
|---|---|---|---|
| `1665.1` | `H-773 A` | `+740-798-803-002-390-530-741+` | target continuing non-`125` `002-390-530-741` row |
| `1665.2` | `H-773 B` | `+700-033-711-335-575-413+` | companion side, not the `002-390-X` target |

So the Vats/Kenoyer route is an object/provenance bridge and a side-label guard. It does not independently fix the target A-side sign sequence.

## Decision

Current status:

`h773_vats_12377_351_side_b_bridge_no_target_a_upgrade`

Use this result as:

- Yes: provenance pressure that local `12377351` points to Vats Plate XCIV item `351`.
- Yes: guardrail against using `H-773B` evidence for target `H-773A`.
- Yes: support for keeping H-773 source-panel-bound rather than metadata-only.
- No: strict source evidence for `002-390-530-741`.
- No: independent proof of sign identities `530` or `741`.
- No: sign value, function, phonetics, language identity, meaning, or translation.

## Consequence

H-773 remains the best in-frame continuing non-`125` pressure row, but it still cannot become a strict anti-`125` exception. The new route makes the side risk clearer: the older Vats/Kenoyer bridge is side B, while the branch-tail stressor is side A.

