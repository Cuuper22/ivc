# Completion Gap Matrix

Date: 2026-05-30

This matrix checks the active goal against current artifacts. It is not a completion claim.

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Continuous adversary: forger | Vector 2 has six effective-unicity controls plus directionality comparator, block, site-balance, policy, influence, and source/formula-family null controls; `002-Y` has 10,000-iteration fixed-bin nulls plus a 10,000-iteration post-hoc partition forger; Vector 4 context associations have context-exact and text-only null scans; Meluhha site-overlap, M-381, M-70, and the directionality no-overlay packet have explicit failure gates. | Partial: active workstreams have forgers, but not a single always-on runner. |
| Continuous adversary: skeptic | Claim ledger records skeptic attacks and unresolved objections; M-381, M-70, branch-gap, and directionality no-overlay blind reviews added hostile source-box failures. | Partial: documented, but not automated continuously. |
| Claim ledger with zero-default accepted counts | `data/claim_ledger/claims.json` now has one accepted structural finding and every live/retracted candidate has provenance. Translations, phonetic values, sign meanings, language identification, and external anchors remain zero. | Present. |
| Effective-unicity methods note | `docs/effective_unicity_methods_note.md` plus generated summaries and curves. | Present. |
| Provenance-tagged sign crosswalk dataset | `data/sign_crosswalk/` and `docs/sign_crosswalk_audit.md`; 1,085 candidate edges, zero accepted edges. | Present as reusable scaffold, not accepted mappings. |
| Findings dossier | `docs/findings_dossier.md` records candidates, retractions, forger records, skeptic attacks, and infrastructure. | Present, still evolving. |
| One-page strongest-result brief | `docs/strongest_result_brief.md` now leads with Vector 2 structural degeneracy, not a source-gated reading. | Present. |
| Novel result usable by an epigrapher | Accepted structural finding: within the fixed `002-861` branch question, `533-717` is the only tested length-1-to-3 unit with support >= 2 whose every occurrence in strict local rows is a terminal tail after `002-861`. It survives exact `text/site/type/symbol` dedup and low-FPR forger controls; worst recorded FPR is 0.006. The two witnesses, `M-376` and `M-391`, are source-visible as same-line terminal-side material, and exact copy-family collapse is rejected. The skeptic boundary is explicit: broad any-prefix search finds 96 repeated terminal bigram cells in strict raw rows and 40 after exact dedup, so the result is fixed-prefix only and carries no value, sound, meaning, or translation. Vector 2, broad `002-Y`, directionality source queues, Meluhha, and Vector 4 remain as candidates/infrastructure or retractions as recorded below. | Achieved for one narrow structural finding; broader decipherment goals remain open. |
| Directionality source-validation infrastructure | The panel-crop repair inventory creates 278 object-panel/signband candidates across 22 CISIs. The cleaned v2d shortlist has 4 target crop candidates, 9 fixed real-negative candidates, and 3 reserves, but the forger rejects it as a packet because the fixed denominator is 9 and crop worlds are mixed. The v2e signband pool widens to 488 candidate crops across 38 public plate-route CISIs, with 4 target CISIs and 27 non-target CISIs having possible-or-strong signband-like geometry. The v2f homogeneous gate tests that surface and fails: strict reuse has 82 candidate rows, compact targets `H-654/M-1310/M-1320`, missing `M-811`, seven original fixed real-negative CISIs, and three target/control source-page collisions; derived top-strip acquisition has 44 rows but is not a blind packet. `docs/effective_unicity_directionality_visual_preflight_failures.md` records the named visual leaks and crop-world failures. | Infrastructure only: no review, no FPR, no claim promotion. |
| Full phonetic readings avoided | No fluent translations or phonetic values accepted. | Satisfied. |
| Language-family likelihood | Internal-only language-family claim is explicitly retracted because of label-symmetry degeneracy. | Negative result only. |
| Effective unicity | Quantified: 1,798 exact sequence families, 571 signs, masked top-1/top-5 0.279591/0.534096, label-symmetry lower bound 4,410.970864 bits. Known-script calibration now includes Linear B Series D and glyph-only SumTablets admin lines. Real-world nonlinguistic calibration shows Pictish and barn-star corpora exceed Indus masked top-1, so masked predictability is not writing-specific. Directionality is sharper: harsh top-edge-removed plus one-edit-family-collapsed Indus stored-win share 0.841096 exceeds current real-world nonlinguistic/ambiguous comparators but remains below known scripts. A later skeptic-control run retracts matched Lipi/Mayig overlap and tiny source-visible `861` support lanes. Block, site-balance, site-profile, direction-policy, influence, and formula-family controls narrow the signal: `site|type|symbol` holdout survives, Mohenjo-daro plus Harappa balanced resampling survives, per-site profiles survive for Mohenjo-daro and Harappa, random row-orientation flips kill the signal, no single row carries it, and metadata source-convention collapse using `site|type|material|symbol|cult|direction` survives at 0.805714 with max admissible null >= observed share 0.005000. The L/R stratum fails harsh controls, so the live directionality wording must stay R/L-dominant and metadata-layer. The source queue ranks 324 major-site harsh rows for source-image validation; the public route probe tested 79 high-pressure rows and found 38 plate-route candidates. M-70 remains the cautionary control: row-level source-visible with a broad order-window continuation candidate, but its 15-item matched-negative blind packet failed promotion with max yes-only FPR 0.714286 and max conservative FPR 0.777778. The directionality no-overlay v1 packet failed promotion with max yes-only FPR 0.333333 and max conservative FPR 0.666667. v2b then failed even under a fixed 12-real-negative denominator because real-negative label leakage and unstable target counts persisted. Full leave-site-out, Lothal, minor-site balanced designs, physical source direction, and source-panel token-box recovery remain unresolved or unvalidated. | Strongest current candidate, narrowed by adversarial comparator and source-validation failure. |
| Anchored constraint collapse | No accepted anchors exist. A stress test forced rejected constraints anyway. The newest stress includes the strict mapped `U17649` onomastic assignment (`002=ur;004=gun3;328=a;001=me;803=luh;415=ha`) and the Brahmi v2 top near-miss set (`817=dhya;527=ra;472=ra;060=ka;061=ra`). Forced together, they fix 11 signs and leave `4310.379871` label-symmetry bits from a `4410.970864`-bit baseline. This is still inadmissible because the object-level assignment failed its readable-script bridge and forger, and Brahmi v3 blocks every v2 family before review. Idealized independent anchors also reduce the floor slowly unless contextual propagation adds information: 1 anchor leaves `4401.813517` bits, 10 leave `4319.511728`, and 50 still leaves `3956.292144`. | Attempted as stress only; no partial reading earned. |
| Sign semantics from context | `520-220-X` context-slot meaning was tested and retracted. Broad Vector 4 context scan also retracted `158-806 / Phyt` after exact-text collapse and family-wise nulls. | No accepted semantic claim. |
| Morphological descent to known script | Two Brahmi back-door gates now exist. Gate 1 parsed 399 Indoskript manuscript rows, selected the first 12 early manuscripts at date <= -100, extracted 457 early Brahmi glyph features, and rejected four actual local Indus source probes under family consistency plus 500-iteration shape nulls. Gate 2 expanded to 36 early manuscripts and 1,342 Brahmi glyph features, tokenized answer-key source crops only when projection-gap segmentation recovered the exact catalog token count, retained 611 source-token features from 61 exact source rows, and tested 83 sign/orientation families with 200 shape-null iterations per token-family sample plus 1,000 label-shuffle iterations per family. All 83 failed; candidate-only rows = 0, accepted phonetic anchors = 0. | Attempted and rejected as a descent/phonetic anchor; stronger infrastructure present. |
| Diffuse external bilingual | Meluhha cuneiform inventory exists; site-overlap anchor was tested and retracted. Continuation-2 hard-anchor test added current CDLI `atf_transliteration` exports: `lu2-sun2-zi-da` appears in 15 artifacts, but only one has any Meluhha line, so the famous Lu-Sunzida shortcut has measured false-positive rate `0.933333` as a Meluhha detector. Continuation-3 attempted actual external sign values by matching cuneiform strings `me-luh-ha`, `ma2-me-luh-ha`, `lu2-sun2-zi-da`, `szu-i3-li2-su`, and `e-me-bal-me-luh-ha` against 29 Mesopotamia/Gulf external rows; all positive candidates failed the pattern-matched forger with null >= observed share `1.000000`, and the duplicate-pattern interpreter-title target had no strict candidate. | Hard named anchor failure plus strict value-attempt failure; no accepted external anchor. |

Additional directionality gate added on 2026-05-29: public-route visibility itself now fails as support. In the frozen top-79 route-probe universe, the 38 public CISI plate-route rows score 0.684211 stored-win share; page collapse gives 0.625000, source-register collapse gives 0.619048, source-convention collapse gives 0.652174, and matched route-label nulls reproduce or exceed the observed route share. This keeps the route layer as acquisition infrastructure only.

Additional Meluhha gate added on 2026-05-29: context-lead matched negatives now supersede the first Lu-Sunzida count. Paginated CDLI querying with `limit=100` finds `szu-i3-li2-su` in 207 returned artifacts and 175 query-line artifacts, only one of which has any Meluhha line, for false-positive rate `0.994286`. `lu2-tukul`, `gurusz`, `nu-banda3`, `ur-{d}lamma`, and `ur-{d}ig-alim` fail as standalone anchors. Nineteen source-side phrases survive only as cuneiform context leads, mostly because they contain `me-luh-ha`; the external retest produces 23 query/site bridge rows, 4 Ur/Susa/Failaka-focus rows, 8 same-site length-pattern matches, and zero object-level bridges. Accepted external anchors remain zero.

BM `120573` object-level route audit added on 2026-05-30: the British Museum record verifies museum no. `120573`, registration `1928,1009.56`, excavation no. `U.7683`, Diqdiqqah/Ur surface find, rectangular steatite stamp seal, and cuneiform/Sumerian inscription. Gadd no. 1 gives only a doubtful cuneiform reading (`sak-ku-shi-?` in ASCII). The five local Ur external rows do not map to it: four are circular and all local rows have Indus numeric sign sequences while BM/Gadd expose cuneiform only. Accepted external anchors remain zero.

Gadd Ur accession bridge audit added on 2026-05-30: the broader Gadd/BM/Penn route now verifies `3898.1/U17649` and `3899.1/U8685`, improving the local object-identity layer. It still fails the micro-bilingual gate: modeled objects split into seven Indus-sequence-only records and one cuneiform-only record, with zero objects combining an Indus sequence and readable cuneiform. Accepted external anchors remain zero.

Gulf Type Indus external queue added on 2026-05-30: Laursen Table 1 external `Gulf INDUS` rows `6-27` are now normalized into `data/meluhha/gulf_type_indus_external_queue.csv`. This does not produce an anchor; it prioritizes the next bridge-acquisition gates: Kjaerum Failaka cat. `279/319`, Mitchell 1986 Ur figures `106-117`, Amiet Susa `1643`, and Sarzec/Heuzey Girsu plate `30.3a-b`.

Object-level onomastic value harness added on 2026-05-30: 12 cuneiform-side Meluhha strings were tested against 30 external Mesopotamia/Gulf rows with parseable signs. The only strict mapped-object value attempt is `ur gun3-a me-luh-ha` against `3898.1/U17649`, proposing `002=ur;004=gun3;328=a;001=me;803=luh;415=ha`. It is rejected because `U17649` is Indus-only, the cuneiform phrase is a separate text, and target-site shuffled nulls reproduce the observed strict mapped same-site pattern attempt with share `0.6857`. Accepted external anchors remain zero.

Additional Brahmi gate added on 2026-05-30: source-token descent v2 expands the descendant-script attempt to 1,342 early Brahmi glyphs and 611 source-token crops. The closest rows (`817=dhya`, `527=ra`, `472=ra`, `060=ka`, `061=ra`) fail the pre-registered shape/label null gates, so the anchored-collapse stress harness records them only as rejected constraints. Accepted Brahmi anchors remain zero.

Brahmi v3 independence gate added on 2026-05-30: all 83 v2 sign/orientation families are blocked before visual review under the stricter rule requiring at least three unique token hashes, three unique CISIs, three unique source paths, duplicate-collapse unanimity, unchanged modal label, and shape/label null shares <= 0.01. Review-packet eligible rows = 0, candidate-only rows = 0, accepted phonetic anchors = 0.

Brahmi real-token low-null autopsy added on 2026-05-31: the 21 rows with real-token impostor share <= `0.01` do not hide a survivor. All 21 fail the original shape-null threshold, 19 also fail the label-null threshold, 11 fail minimum source-token independence, and no row passes both minimum independence and duplicate-collapse unanimity. Review-packet eligible rows remain 0.

## Current Completion Judgment

The final artifact set is largely present, and the central success condition has a narrow accepted hit: `accepted_struct_002_861_533_717_restricted_tail_2026_05_29`. The strongest honest output is now “one structural tail formula earned, no reading earned.” Vector 2 remains the most defensible corpus-level instrument, and broad `002-Y` remains the strongest larger grammar candidate still awaiting source normalization.

Next best moves:

1. Try to widen the accepted `002-861-533-717` finding with source-normalized exact token boxes, but only if matched negatives stay low-FPR.
2. Source-normalize enough of the strict Lipi layer to make the Vector 2 result independent of the T3 metadata objection.
3. Build a future directionality packet only after the v2f gates are met: 12 fixed real signband-negative CISIs, 12 real nonlinguistic nulls, 32 fixed-seed synthetic nulls, no target/control crop-world mismatch, no source-page cue leakage, and at least three blind reviewers.
4. Continue Meluhha matched controls beyond literal site overlap.
5. Treat broad source-box visual packeting as invalid until a negative-control packet gets low false-positive rates.
6. Keep `M-12/BG001` as an audit target only unless a future higher-resolution source or independent source family removes the `BG009`-style stable-overcount false-positive risk.
