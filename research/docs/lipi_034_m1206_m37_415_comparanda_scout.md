# Lipi 034 M-1206 / M-37 415 Comparanda Scout

Date: 2026-05-25

This note is a scout: it goes looking for comparanda — other objects carrying the same inscription, which can be compared against the one in hand. Until now the sign `415` in this frame rested on a single Mohenjo-daro seal, and one object cannot tell you whether a shape is stable or idiosyncratic. The scout finds where public source pages exist for the rest. It sorts nothing and settles nothing; it only builds the input list for the next test.

## Question

The M-1206/M-37 blind visual probe — a comparison run with the crops label-stripped so the reviewer could not know which object was the target — showed broad pattern recurrence but fine-form uncertainty. Is M-37's local `415` side a one-off source form, or can we build a source-visible fine-form bin?

This scout searches exact local rows:

```text
+520-220-415+
```

## Result

There are 17 exact local `+520-220-415+` rows in `metadata_filtered.csv`.

Composition:

- 1 Mohenjo-daro seal: `M-37`.
- 16 Harappa rows, mostly tablets.

Public CISI 1/2 scout result:

- Source-visible candidate pages were found for `M-37`, `H-346`, `H-786`, `H-938`, `H-939`, `H-940`, and `H-941`.
- Register/data-page-only OCR routes were found for `H-319` and `H-364`.
- No usable public CISI 1/2 OCR route was found in this pass for the remaining exact rows: `H-1326`, `H-2148`, `H-2240`, `H-2146`, `H-2149`, `H-1992`, and `H-1284`.

Downloaded source pages:

```text
tmp/m1206_m37_blind_visual_comparanda/cisi_india_n265_h346_w2000.jpg
tmp/m1206_m37_blind_visual_comparanda/cisi_pakistan_n359_h786_w2000.jpg
tmp/m1206_m37_blind_visual_comparanda/cisi_pakistan_n374_h939_h940_w2000.jpg
```

## Immediate Visual Use

These are not yet adjudicated as `415` fine-form evidence. They are the next blind-binning inputs.

The useful thing is that the exact `+520-220-415+` class is no longer just M-37:

- `H-346` gives a public India-volume tablet source page.
- `H-786` gives a public Pakistan-volume tablet source page.
- `H-938` through `H-941` give a dense public Pakistan-volume page with neighboring exact-class candidates.

This creates the first real way to test whether the M-37 rake/stemmed-triangle profile is stable, whether Harappa tablets use a different fine-form convention, or whether local `415` is a mixed bin.

## Quarantine

To quarantine is to hold findings aside as unusable evidence. No inference is accepted from this scout:

- No `034 = 415`.
- No `415` value.
- No cross-site generalization from Mohenjo-daro seal to Harappa tablets.
- No tablet/seal equivalence.
- No translation.

The next experiment must crop these pages and run blind fine-form sorting before any `415` sign-family statement is allowed.

