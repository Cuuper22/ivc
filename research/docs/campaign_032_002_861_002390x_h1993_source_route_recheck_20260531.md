# H-1993 source-route recheck for `002-390-X`

Date: 2026-05-31 America/Los_Angeles

Status: active source/sign gate, not goal completion.

## Target

H-1993 is the unbound repeated `095` witness inside the live `002-390-X` frame:

- Local row: `744.2`
- Object: `H-1993`
- Site/type: Harappa `TAB:B`
- Excavation hook: `H96-2769Figure 17.07`
- Material/shape: clay, rectangular, plano-convex
- Dimensions: `29.2 x 10.7 x 6.2`
- Local text: `+740-000-220-004-002-390-095+`
- Structural role: `004 -> 002-390 -> 095 -> <END>`

The question is whether H-1993 can be promoted from a transcription-level route clue into a source-image witness for the non-`125` terminal branch `002-390-095`.

## Route evidence

The public Harappa ESM2 PDF exposes `ICIT 744 (H-1993)` in the relevant sign-string neighborhood. That is useful, but it is not an artifact image. It keeps a route to the object open and justifies keeping H-1993 hot for acquisition; it does not prove the sign sequence from a photograph, plate, or crop.

The current public and local searches did not locate an artifact image for H-1993:

- Exact/public queries for `H-1993`, `H96-2769`, `H96-2769 Figure 17.07`, and `H-1993 Figure 17.07 Harappa` returned the ESM2 transcription route but no usable source panel.
- Local `rg` checks found metadata rows, notes, and generated reports, not an H-1993 source crop.
- A local `harappa_appendices.pdf` candidate under `tmp/h2147_figure_reconcile` is HTML bytes, not a PDF source.
- A local `Kenoyer Meadow 2010` PDF text extraction did not match `H1993`, `H-1993`, `H96-2769`, or `Figure 17.07`.

## False-bridge guard

`Figure 17.07` cannot be used alone as a decisive bridge. Local metadata also assigns a `Figure 17.07` hook to H-1803:

- `562.1`, `H-1803`, excavation `H94-2193Figure 17.07`, text `]000-700[`
- `562.2`, `H-1803`, excavation `H94-2193Figure 17.07`, text `]590-000[`

So the next route has to bind H-1993 specifically inside Figure 17.07, or bind excavation id `H96-2769`, or recover a plate/caption where H-1993 itself is visible. A naked figure number is too loose.

## Decision

Current status:

`source_route_triaged_supplement_only_no_image`

Addendum, 2026-05-31:

A fresh image-binding recheck tightened the acquisition target but did not promote H-1993. The public Harappa page and linked Kenoyer/Meadow CISI 3 excerpt show that the relevant full-volume material should be in CISI 3.1's HARP `H-1521` to `H-2583` photograph section, pages `207-360`, and Harappa basic-data section, pages `423-441`. The public PDF itself is only a 17-page essay/front-matter excerpt, not those plates/data pages. Fresh public searches, local text searches, and local image filename checks still found no H-1993 artifact image or caption.

Addendum status:

`h1993_cisi3_plate_range_identified_no_image_binding`

Artifact: `docs/campaign_032_002_861_002390x_h1993_image_binding_recheck_20260531.md`

Report: `data/open_prototype/reports/campaign_032_002_861_002390x_h1993_image_binding_recheck_20260531.csv`

Use H-1993 as:

- Yes: acquisition target for actual Figure 17.07 / H96-2769 source image.
- Yes: transcription-level pressure that `002-390-095` is a real local row neighborhood.
- No: strict source-image evidence.
- No: independent token-box evidence for `095`.
- No: sign value, phonetics, function, language identity, meaning, or translation.

## Consequence for `002-390-X`

The `095` branch remains mixed:

- `M-71` is the strict source-visible `002-390-095` comparator from the checkpoint.
- `H-1993` is a second local `002-390-095` row, but source-route only.

So `095` can stay in the branch ecology, but only one `095` witness is strict. The H-1993 route gives neither side what it wants: the positive model cannot claim a source-normalized repeated terminal `095` branch, and the adversarial model cannot dismiss the row as nonexistent. It is alive, but not counted.

Next gate: find the actual H-1993 image/caption for `H96-2769Figure 17.07`, or keep H-1993 out of strict counts and move source pressure to Dholavira `4237.1` / `705`.
