# 032-002-861 / 002-390-X Linguistic Decision Gate

Date: 2026-05-30

## Question

After source normalization, what is the actual linguistic question for `002-390-X`, and what would demote `125` back to non-evidence?

## Current Position

The live object is the `002-390-X` branch slot, not `125` by itself.

`125` is the strongest current branch member because `M-119` and `M-735` are strict public source-visible witnesses, with `Sktd-1` as a weaker panel-bound candidate. But `M-70` and `M-71` are strict source-visible non-`125` continuations, so `125` is not required after `002-390`.

The branch contrast is therefore:

- `125`: continuation-bearing branch candidate; strict visible witnesses `M-119/M-735`, weak support `M-38`, panel-bound support `Sktd-1`.
- `095`: terminal non-`125` comparator; strict visible witness `M-71`, source-dark second row `H-1993`.
- `692`: terminal non-`125` comparator; strict visible witness `M-70`.
- `705`: potentially important repeated non-`125` comparator, but source-dark in this packet.

No value, phonetic reading, language identity, or translation is accepted.

## Ranked Linguistic Hypotheses

1. Branch-choice slot after `002-390`.

   `002-390` may open a position where the next sign selects a continuation path. `125` is currently the largest and best source-supported branch member, while `095` and `692` prove plurality.

   Decisive experiment: token-box `M-119/M-735` against `M-70/M-71`, then route at least one `705` witness. The model strengthens if `125` rows keep taking continuation tails while visible non-`125` rows close or take different tail classes.

2. `125` as a subframe-bearing branch, not a general suffix.

   `125` may introduce further material rather than close the phrase. The live subframes are `125->632 032` and `235->002-390->125`.

   Decisive experiment: build all source-visible `125->632` and non-`125->632` environments. The model strengthens only if `632-032` clusters specifically after `002-390-125` across independent source families.

3. Previous sign conditions branch choice.

   `235` before `002-390` currently points toward `125`, while the `004` split could become sharper if `H-1993` is routed against `Sktd-1`.

   Decisive experiment: source-visible `PREV -> 002-390 -> X` matrix, starting with `235` and `004`. The model strengthens if previous signs predict branch choice after family/source collapse.

## Demotion Rules

`125` returns to non-evidence if any of these happen:

- Independence collapse: `M-119`, `M-735`, `M-38`, and `Sktd-1` reduce to one copied/near-copied formula family, workshop habit, iconographic class, or source tradition.
- Immediate-order failure: token boxing fails to preserve same-side immediate `002 -> 390 -> 125` in either strict witness. With only two strict witnesses, one loss is enough to demote.
- Formula-tail collapse: `125` occurs only inside closed longer formulas such as `125-632-032` or `235-002-390-125`, with no independent recurrence across different tails.
- Matched non-`125` context: strict visible rows show the same local frame with non-`125` continuations, such as `235-002-390-095/705/692`, or a cleaner `004-002-390-095` than `Sktd-1`.
- Source-normalized ecology expansion: repeated strict non-`125` branches (`095`, `705`, `692`, or others) become as independent or more independent than `125`, while `125` remains two family-linked rows.

## Next Gate

Run token-boundary adjudication on `M-119`, `M-735`, `M-70`, and `M-71`, with `Sktd-1` downweighted and `M-38` excluded unless a sharper source appears.

The required output is not a reading. The required output is whether source-visible inscriptions preserve a real branch slot after `002-390`, and whether `125` survives as an independent branch member after side/order and formula-family collapse.
