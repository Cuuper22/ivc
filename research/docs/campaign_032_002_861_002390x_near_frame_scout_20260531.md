# 002-390-X near-frame scout

Date: 2026-05-31 America/Los_Angeles

This note is a completeness check. Earlier work built a clean set of 15 rows where the sign pair `002-390` appears with a sign right after it. This scout asks whether that set missed anything — damaged rows, rows with open readings, or rows where a sign sits between `002` and `390` — that could change the branch analysis. It found nothing new, and recording that is the point.

Status: active campaign gate, not goal completion. (A "gate" is a check that must pass before evidence is allowed into the analysis.)

## Question

Did the clean 15-row `002-390-X` predecessor gate miss any damaged/open adjacent rows or gapped near-frame rows that could create a same-predecessor branch contrast? ("Adjacent" means `002-390` with the branch sign directly next to it; "gapped" means a sign intervenes, as in `002-X-390`; a "same-predecessor branch contrast" means two rows that share the sign before `002` but diverge after `390`.)

No value, phonetics, language identity, function, sign meaning, or translation is accepted.

## Outputs

- Runner: `data/open_prototype/tools/campaign_032_002_861_002390x_near_frame_scout_20260531.mjs`
- Adjacent rows: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_adjacent_rows.csv`
- Gapped rows: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_gapped_rows.csv`
- Adjacent predecessor summary: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_adjacent_prev_summary.csv`
- Gapped predecessor summary: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_gapped_prev_summary.csv`
- Decisions: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_decisions.csv`
- Summary: `data/open_prototype/reports/campaign_032_002_861_002390x_near_frame_scout_20260531_summary.json`

## Result

Status: `near_frame_scout_no_hidden_matched_prev_rescue_no_values`.

The scan covered all `5,679` rows in `metadata_filtered.csv`.

It found:

- `15` adjacent `002-390` rows.
- `0` damaged/open adjacent `002-390` rows.
- `3` multi-row adjacent predecessor groups, exactly the known set: `004`, `032`, and `235`.
- `2` gapped `002-X-390` near-frames.
- `0` gapped same-predecessor multi-branch groups.

## Adjacent Multi-Row Predecessor Groups

| Prev before `002` | Rows | Branches | Current consequence |
|---|---:|---|---|
| `004` | 2 | `095`, `125` | Still blocked: H-1993 is route-only; Sktd-1 is panel-bound side-pair visible but not strict. |
| `032` | 2 | `692`, `590` | Still blocked: M-70 is strict; `3335.1` is object-ID blocked and formula-family pressured. |
| `235` | 2 | `125` only | Same-branch tail split, not branch alternation. |

There are no hidden damaged/open adjacent rows that add a new same-predecessor branch alternative.

## Gapped Near-Frames

| Object | Frame | Status |
|---|---|---|
| K-89 | `350 -> 002-595-390 -> 000` | Gapped `002-X-390`; not an adjacent `002-390-X` branch and not repeated in a matched predecessor group. |
| Dholavira `4348.1` | `000 -> 002-861-390 -> <END>` | Gapped `002-X-390`; not an adjacent `002-390-X` branch and not a bridge to `4237.1`. |

The `4348.1` result reinforces the earlier ICIT 4348 guard. Local `4348.1` is `]740-142-000-002-861-390[`, not a `002-390-705` row and not a binding bridge for Dholavira `4237.1`.

## Decision

The predecessor-gate search space is now tighter:

- The adjacent clean set is exactly the 15-frame set already used.
- There is no damaged/open adjacent row that creates a new matched strict split.
- Gapped `002-X-390` rows do not rescue the branch model or add matched alternatives.
- `ICIT 4348` remains guarded out of the Dholavira `4237.1` route.

The positive model keeps only the existing branch-tail ecology pressure — the observed variety of signs following the branch point. It does not gain a same-predecessor proof.

Accepted value, phonetics, language identity, function, sign meaning, and translation remain 0.
