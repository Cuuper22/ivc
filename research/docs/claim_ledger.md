# Claim Ledger

Date: 2026-05-30

The authoritative machine-readable ledger is `data/claim_ledger/claims.json`.

Accepted counts:

| Claim class | Count |
| --- | ---: |
| translations | 0 |
| phonetic_values | 0 |
| sign_meanings | 0 |
| language_identification | 0 |
| structural_findings | 1 |
| external_anchors | 0 |

## Accepted Structural Finding

Claim ID: `accepted_struct_002_861_533_717_restricted_tail_2026_05_29`

Accepted wording: within the fixed `002-861` branch question, `533-717` is the only tested length-1-to-3 unit with support >= 2 whose every occurrence in strict local rows is a terminal tail after `002-861`. The two witnesses are `M-376` and `M-391`; both are source-visible as same-line terminal-side material, and current source-family review rejects exact copy-family collapse.

Forger record: `data/open_prototype/reports/campaign_032_002_861_restricted_tail_forger_summary.json` tests strict raw rows and exact `text/site/type/symbol` dedup rows. Worst recorded false-positive rate is `0.006`; the strongest context shuffles are `0.0002` global and `0.0010` within `site/type/symbol` for strict raw rows.

Skeptic boundary: broad post-hoc prefix search finds 96 repeated terminal bigram cells in strict raw rows and 40 after exact dedup, so the claim is fixed to the prior `002-861` branch question only. It does not accept exact source-normalized `861/533/717` token boundaries, sign value, sign meaning, phonetics, language family, external anchor, or translation.

## Live Candidates

The strongest current candidate is structural, not phonetic: in the strict deduplicated all-`002` layer, signs after `002` split into a high-terminal pole and a continuation pole. The fixed-bin forger pass now runs 10,000 iterations per null model against five nulls and does not reproduce the 499-row all-`002` effect. A harsher post-hoc partition forger then lets every null discover its own best high-terminal versus low-terminal split. The broad all-`002` best split is `817/820` versus `390/368/031/220`, with z 13.211692, terminal-rate gap 0.954023, and worst FPR 0 for both z and gap across 10,000 iterations per null model.

The skeptic pass preserves the broad all-`002` effect under removal attacks and family-blocked prediction. It also records an important failed attack: exact right-edge matching is tautological for terminality because `text_len` plus `idx_002` already fixes whether Y is terminal. The smaller adjacent `032-002` scope fails the new post-hoc partition forger, with worst FPR 0.5205 for z and 0.7552 for gap, so it cannot be promoted as its own partition claim.

A source-normalization queue now makes the next gate concrete. In the same 499-row strict all-`002` scope, `817/820` have four grade >= 2 source hooks across 174 closure-pole rows. After the corrected supplemental public CISI route acquisition, the branch pole has 14 grade >= 2 route hooks across 43 rows: `390` has 2, `368` has 5, `031` has 4, and `220` has 3. The demoted `H-44` hit was an OCR prefix/split-label trap from nearby `H-449`-style labels. The first branch-gap packet then produced a useful but non-promotable audit lead: `BG001/M-12` was counted as 10 visual units by all three blind reviewers against 9-token Lipi and Mayig witnesses, but same-packet scoring negative `BG009/M-654` reproduced stable over-counting with observed control rate 1/6. That does not promote the metadata-layer forger result into a source-normalized proof, because the new branch routes are not stable under matched negative controls, token-boxed, direction-checked, or source-family reviewed.

This is not accepted yet. The main unresolved attack is source normalization: the result is still in the T3 Lipi metadata/sign layer, and the decisive rows are not yet fully source-boxed with side/direction checks outside the quarantined metadata layer. The older exact named-bin wording is also descriptive; `861` remains closure-heavy but leaky in that model, not part of the strict post-hoc best two-pole partition.

The new Vector 2 candidate is also structural: the strict exact-sequence-collapsed Lipi working corpus shows local context constraint, while remaining far from phonetic unicity. At full coverage the corpus has 1,798 exact sequence families, 8,212 tokens, and 571 signs. Leave-one-row-out masked top-1 accuracy is 0.279591, with full-coverage FPR 0 across six tested forger controls at 100 iterations per control. Known-script calibration now includes Linear B Series D and SumTablets. Under the IVC length cap, clean Linear B bidirectional masking gives top-1/top-5 = 0.435897 / 0.698006, while source-gapped sequence leave-out gives 0.294314 / 0.638796. The glyph-only SumTablets administrative-line comparator, with transliteration and glyph names hidden, gives 0.171753 / 0.373681 over 1,798 selected lines and 358 glyphs, beating its own five matched controls with max null >= observed share 0. Indus is in the neighborhood of the harsher Linear B gapped task and above this SumTablets glyph-only line sample, but it remains below clean Linear B masking and is burdened by 571 signs versus 91 Linear B tokens and 358 SumTablets glyphs in the capped comparators. A structured nonlinguistic rerun cuts off one bad shortcut: the older broad bidirectional predictor has observed top-1 0.325865, but administrative, emblem, and mixed admin/emblem generators exceed it with null means 0.472924, 0.443351, and 0.415069. A real-world Sproat 2014 comparator cuts off a second shortcut: Pictish stones score 0.450292 / 0.614035 and barn stars / hex signs score 0.367123 / 0.673973 under the same masked-symbol task, both above Indus top-1. The unanchored phonetic label-symmetry lower bound is still log2(571!) = 4,410.970864 bits, so this result rejects internal-only language-family likelihood rather than supporting it.

A directionality comparator is now the sharper part of this candidate. It scores stored order versus reversed order with the tested row removed from the bigram model. In the strict exact-collapsed Indus scope, stored order wins 0.947720 of rows. In the harsh top-10-edge-removed plus one-edit-family-collapsed scope, stored order still wins 0.841096 of rows with max null >= observed share 0. Current real-world nonlinguistic and ambiguous comparators do not reach that harsh Indus value; Pictish is the highest at 0.802139. Known scripts sit higher: Linear B Series D 0.996403 and SumTablets 0.935484. Mayig P-namespace exact-collapsed records also show high stored-order asymmetry at 0.943750, but the harsh Mayig scope drops to 0.666667 over only 24 rows. A later skeptic-control run retracts the matched Lipi/Mayig overlap support lane: the exact overlap has Lipi-side 0.937500 and Mayig-side 0.929134, but after top-10 edge removal it collapses to Lipi 0.363636 and Mayig 0.473684, both with max null >= observed share 1. The tiny source-visible `861` directionality probe is also retracted as support because its observed 1.000000 stored-win share has max null >= observed share 1. A block-control run keeps the broad directionality candidate alive under `site|type|symbol` holdout: harsh one-edit collapse gives stored-win share 0.827397 with max null >= observed share 0, and register-edge-family collapse gives 0.835227 with max null >= observed share 0.005000. Full leave-site-out is not clean in harsh scopes, with max null >= observed share 0.100000 and 0.265000 respectively. A site-balanced resampling run survives for Mohenjo-daro plus Harappa at median stored-win share 0.803571 and max paired null >= observed share 0.003, but becomes null-compatible when Lothal is added at equal weight or when the top five sites are balanced. Per-site profiles show Mohenjo-daro and Harappa each carry internal directionality, while Lothal does not in the current harsh subset. Direction-policy controls show `R/L`-only rows preserve the signal and random row-orientation flips destroy it, while physical source direction remains unvalidated. Influence controls show the signal is not dependent on one row: largest supportive row delta is 0.005931 and largest absolute row effect is adverse, L-115 at 0.010552. The largest coarse family effect is `Mohenjo-daro|SEAL:R|None` at 0.028975. Formula-family controls add a stronger metadata attack: with `source_convention_key = site|type|material|symbol|cult|direction`, harsh leave-source-convention-out scoring gives stored-win share 0.832877 with max admissible null >= observed share 0.020000, and harsh source-convention collapse gives 175 rows, stored-win share 0.805714, and max admissible null >= observed share 0.005000. This narrows, but does not remove, the source-family objection: exact L/R is null-compatible at 0.520000 and harsh L/R has only 11 rows, stored-win share 0, and max null >= observed share 1. The source-validation queue ranks 324 major-site harsh rows and shows the practical bottleneck: 223 need a public route or source request, 94 have only non-source-grade catalogue hints, and only five initially had public-route or crop-review hooks. The first queue source pilot upgrades M-70 to row-level source-visible with a broad order-window continuation candidate, not to an accepted source-normalized reading. Its matched-negative blind token-box packet failed promotion with two independent reviews: max yes-only FPR 0.714286, max conservative FPR 0.777778, and one target only uncertain for one reviewer. The public CISI route probe converts part of the route bottleneck into a visual-admissibility queue: among 79 top source-route rows, it finds 38 public plate-route candidates, one data/register-only route, and 40 still not found. Manual visual triage of nine top hits yielded four attempted no-overlay target crops, `H-654`, `M-1310`, `M-1320`, and `M-811`. v1 failed after review: max yes-only FPR 0.333333, max conservative FPR 0.666667, seven packaged unique real negatives below the forger floor of ten, one leaked negative label, hard scoring-negative false positives, and unstable target counts. v2b reached 12 real negatives and zero duplicate image hashes but failed too: real denominator rows leaked labels, reviewer C had target-like uncertainty on 2/12 real negatives, and target counts were unstable. Accepted claim increment remains zero.

This also is not accepted yet. The main unresolved attacks are T3 source status, exact-only dedup for the masked task, still-limited known-script comparator coverage, lack of source-normalized confirmation that the directionality signal survives outside the Lipi metadata layer, the need to rebuild any overlap test crosswalk-blind, failed full-site holdout in harsh scopes, null-compatible minor-site balanced samples, source-normalized per-site validation, physical source-direction validation after the failed M-70 packet, physical source-family/workshop-copy concentration checks, and the failed harsh L/R stratum.

The source-visible `032-002-Y` candidate has been retracted. The witness matrix found eight source-visible rows across target `240-220-032`, non-240 `A-220-032`, and outside-`A-220` contexts, but the matched-negative blind adjudication failed. Two blind reviewers recovered zero positives as confident positives while producing yes-only false-positive rates of 0.400000 and 0.300000 on negatives. The source-route matrix remains useful infrastructure; it no longer supports a live structural claim.

The replacement branch `002-390-X` ecology recheck is now in place at `docs/campaign_032_002_861_002390x_branch_sign_ecology_20260531.md` and `data/open_prototype/reports/campaign_032_002_861_002390x_branch_sign_ecology_20260531_summary.json`. It independently re-derives 15 adjacent `002-390` frames from `metadata_filtered.csv`: `125` is the largest branch with four rows and is always continuing in-frame, while `095`, `692`, and `705` are terminal in-frame. The adversary blocks promotion: H-773 is a continuing non-`125` pressure row but only boxed-compatible, `3335.1` remains object-ID blocked, H-1993 is supplement-route-only, Dholavira `4237.1` is not source-bound, and `125` has terminal non-frame occurrences. This keeps `002-390-X` live as a constructional ecology target, not an accepted structural claim.

## New Retraction

`retracted_source_visible_032_002_y_packet_2026_05_29`: the visual packeting method finds plausible `032-002-Y` calls in negative controls at unacceptable rates. This is exactly what the forger is for. Dead candidate, useful corpse.

`retracted_directionality_source_visible_and_overlap_support_2026_05_29`: source-visible `861` terminal/tail directionality and matched Lipi/Mayig overlap cannot support the broad directionality candidate. The `861` probe has 12 target-selected rows, stored-win share 1.000000, and max null >= observed share 1. The overlap exact headline weakens under Mayig-side nulls and then fails under top-edge removal: Lipi side 0.363636 and Mayig side 0.473684, each with max null >= observed share 1.

`retracted_m70_source_boxed_order_window_promotion_2026_05_29`: the M-70 source-boxed order-window promotion failed its matched-negative blind gate. Two reviewers scored the 15-item packet; the maximum yes-only false-positive rate on scoring negatives was 0.714286 and the maximum conservative false-positive-or-uncertain rate was 0.777778. M-70 remains row-level source-visible with a broad order-window continuation candidate, not an accepted source-boxed token or direction claim.

`retracted_directionality_no_overlay_source_normalized_packet_2026_05_29`: the H-654/M-1310/M-1320/M-811 no-overlay source-normalization packet failed its matched-negative blind gate. Two reviewers scored the 15-item packet; max yes-only FPR was 0.333333 and max conservative FPR was 0.666667. The packet was below the real-negative denominator floor, leaked `D006/M-525`, produced hard scoring-negative false positives, and did not recover stable target counts.

`retracted_directionality_no_overlay_v2b_source_normalized_packet_2026_05_29`: the stricter v2b packet fixed the earlier duplicate-hash and 12-real-negative denominator defects, then failed anyway. Three reviewers scored the 23-item packet; max yes-only FPR on the fixed denominator was 0, but max conservative FPR was 0.166667, real denominator rows leaked labels, and target token counts were unstable.

`retracted_directionality_no_overlay_v2c_preflight_packet_2026_05_29`: v2c fixed the DjVu OCR five-coordinate parser bug and kept a 12-real-negative denominator, but failed before blind review. Visual preflight showed the underlying rows were still page-context crops with catalogue/page cues after OCR masking, so the packet is not reviewer-ready and has no false-positive claim score.

`retracted_directionality_panel_crop_v2d_packet_readiness_2026_05_29`: the cleaned v2d shortlist is not packet-ready. Its fixed real-negative denominator is 9 because three rows remain reserves, and the crop worlds still mix target signband strips with tablet/object and animal/icon control panels. It has no blind review and no false-positive claim score.

`retracted_directionality_homogeneous_signband_v2f_packet_readiness_2026_05_29`: the homogeneous v2f signband-strip gate failed. Strict v2e reuse has compact target strips for `H-654`, `M-1310`, and `M-1320`, but not `M-811`; it has only seven original fixed real-negative CISIs and source-page collisions. The derived top-strip lane is acquisition-only and still misses `M-1320`/`M-811` before visual review.

`retracted_directionality_public_route_visibility_support_2026_05_29`: public CISI route availability cannot support promotion of the directionality candidate. In the frozen top-79 route-probe universe, the 38 public-route rows score 0.684211 stored-win share, page collapse drops them to 0.625000, `site|type|symbol|direction` collapse drops them to 0.619048, and matched route-label nulls reproduce or exceed the route subset score with null >= observed share 1.

`retracted_vector4_158_806_phyt_context_association_2026_05_29`: the broad Vector 4 context scan found a tempting `158-806 / Phyt` association, but exact-context support was inflated by repeated exact text under different context/material metadata. Text-only collapse drops support below the scan threshold, and the family-wise null share is 1. Treat it as an acquisition lead, not a sign meaning.

`retracted_meluhha_context_leads_as_external_anchors_2026_05_29`: paginated CDLI matched negatives and external bridge retesting reject the current Meluhha context leads as external anchors. `szu-i3-li2-su` returns 207 artifacts and 175 query-line hits, with only one Meluhha-bearing query-line artifact, FPR `0.994286`; the external retest has 23 query/site rows, 4 Ur/Susa/Failaka-focus rows, 8 same-site length-pattern matches, and zero object-level bridges.

`retracted_bm120573_as_external_phonetic_anchor_2026_05_30`: BM `120573 / U.7683 / 1928,1009.56` is now verified as a real object-level cuneiform route but rejected as an external phonetic anchor. It is rectangular and cuneiform-only in the BM/Gadd source surface; the five local Ur external rows are not mapped to it and carry Indus numeric sign sequences. Accepted external anchors remain zero.

`retracted_gadd_ur_accession_bridge_micro_bilingual_2026_05_30`: the broader Gadd/Penn Ur object audit verifies `3898.1/U17649` and `3899.1/U8685`, but rejects the micro-bilingual route. The modeled surface has seven Indus-sequence-only objects, one cuneiform-only object, and zero objects combining an Indus sequence with readable cuneiform. Accepted external anchors remain zero.

`retracted_object_level_onomastic_value_attempts_2026_05_30`: the direct onomastic value harness tried 12 Meluhha strings against 30 external Mesopotamia/Gulf rows. The only strict mapped-object value attempt was `ur gun3-a me-luh-ha` against `3898.1/U17649`, proposing `002=ur;004=gun3;328=a;001=me;803=luh;415=ha`. It fails because `U17649` is Indus-only, the cuneiform phrase is a separate text, and target-site shuffled nulls reproduce the observed strict mapped same-site pattern attempt with share `0.6857`. Accepted external anchors remain zero.

`retracted_brahmi_source_token_descent_v2_2026_05_30`: the expanded Brahmi source-token gate rejects every tested descendant-script near-match. It retained 1,342 early Brahmi glyph features and 611 source-token features, tested 83 sign/orientation families, and produced zero candidate-only rows and zero accepted anchors. The top near-misses (`817=dhya`, `527=ra`, `472=ra`, `060=ka`, `061=ra`) fail the shape/label null thresholds.

`retracted_brahmi_independent_source_token_gate_v3_2026_05_30`: the duplicate/CISI independence preflight blocks all 83 v2 sign/orientation families before visual review. Review-packet eligible rows, candidate-only rows, and accepted phonetic anchors are all zero. The v2 headline near-misses are not independent: `817`, `472`, and `060` have one unique token hash and one CISI; `527` and `061` have two unique token hashes and one CISI.

## Infrastructure Audit

The sign crosswalk scaffold now has an audit. It passes hygiene checks with caveats: 1,085 candidate edges, zero accepted edges, zero duplicate primary keys, zero dangling references, and zero evidence hash/path errors. The audit explicitly keeps high-pressure edges such as `002 -> P122`, `032 -> P145`, and `817/861 -> P385` as review targets, not accepted mappings.

Vector 1 now has a reproducible cuneiform-side Meluhha inventory. `data/meluhha/cuneiform_attestations_expanded.csv` contains 16 source-side rows from 10 requested CDLI/BDTNS pages, with 18 successful fetches recorded in `data/meluhha/cuneiform_fetch_log.csv`. This is infrastructure only: accepted external anchors remain zero until a proposed Meluhha-Indus pairing gets matched controls, a measured forger false-positive rate, and a skeptic pass.

The first Meluhha-Indus join surface is also in place. `data/meluhha/meluhha_indus_join_surface.csv` contains 25 literal site-overlap lanes across Ur, Girsu, Tello, and Nippur. Chronology is not scored because the current external-object table does not expose dates, and every row is marked `join_surface_not_claim`. The forger rejects site overlap alone as evidence: random site aliases from all external sites reproduce or exceed the observed 25 join rows in 0.815100 of iterations, and Mesopotamia-only site aliases do so in 0.933800.

The first focused Vector 4 rerun is negative. `data/open_prototype/reports/campaign_520_220_x_context_slot_null_summary.json` tests whether X in `520-220-X` predicts closure/continuation beyond site/type register. It does not: raw third-slot accuracy is 0.687500 versus `site|type` 0.812500, and exact-text-collapsed third-slot accuracy is 0.781250 versus `site|type` 0.906250. The frame remains useful, but this context-slot meaning is retracted.

The broad Vector 4 context-association scanner is now in place. `data/open_prototype/tools/vector4_context_association_scan.mjs` tests sign units against type, material, symbol, cult, and compound context labels under context-exact and exact-text collapse. The first run produces no accepted association. `407` with Mohenjo-daro copper `TAB:C` is register-compatible under the harsh nulls, and `158-806 / Phyt` dies under exact-text collapse.

The replacement branch targeted Vector 4 null runner is now in place at `docs/vector4_targeted_context_nulls_20260531.md` and `data/open_prototype/reports/vector4_targeted_context_nulls_20260531_summary.json`. It tests eight fixed tempting unit/context pairs with 2,000 iterations per null model. The register-looking leads (`407`/Copper, `407`/`TAB:C`, `061-845`/Copper, `400`/`TAB:B`) are reproduced by shuffling units within site/type with worst fixed-pair null share `1.000000`. The only residue is `158-806 / Phyt`, but exact text collapse leaves support `3`, below the broad scanner support floor. No sign meaning is accepted.

The broad `002-Y` partition source queue is now in place. `data/open_prototype/reports/campaign_002_y_partition_source_queue_summary.json` keeps the post-hoc forger scope fixed and ranks acquisition rows for source-normalization. The corrected supplemental public CISI branch-gap acquisition closes the route-inventory gap for `368`, `031`, and `220`, while keeping all of those rows at route-candidate status only. It also records the `H-44` demotion as a route-failure correction. The new bottleneck is source-visible token boxing, physical side/direction checks, source-family/copy review, and matched negatives.

The first branch-gap source-box packet has now failed its stage-1 blind token-count gate. `data/open_prototype/reports/campaign_002_y_branch_gap_blind_packet_summary.json` packages 14 blind images: three primary targets, three backup targets, six scoring negatives, and two quarantines. The contact sheet was visually checked and the scoring/target crops were tightened to remove object-label leakage. Three blind reviewers were scored in `data/open_prototype/reports/campaign_002_y_branch_gap_blind_review_summary.json`. Promotion decision: `not_promotable_stage1_only_no_claim_increment`, because target and backup rows do not have stable exact token counts. The diagnostic residue is useful but bounded: all three reviewers counted `BG001/M-12` as 10 visual tokens against the 9-token catalog string, then a direct audit found Lipi and Mayig both at 9 tokens and a scoring-negative stable-overcount control at `BG009/M-654` with observed rate 1/6. Main audit artifact: `docs/campaign_m12_token_count_audit.md`. `M-12` stays an audit target, not accepted evidence.

The v2 source-box negative-control lane is now partially sourced. `data/open_prototype/reports/source_box_negative_control_v2_public_routes.csv` records public CISI routes for M-381, M-124, and M-38. `data/open_prototype/reports/source_box_negative_control_v2_source_status.csv` collapses the route and panel manifests into one target-level status table. Human visual inspection of the generated contact sheet put M-381 in the strongest bucket, so it was packaged as a stage-separated one-item adjudication packet: blind tokenization first, answer-key alignment second. The packet failed the clean-negative gate. Three blind reviewers returned token counts of 9, 13, and 9 against the seven-token catalog answer key, and all three reported skip/merge risk in crowded regions. M-381 remains an ambiguity stress packet, not an admissible negative control. M-124 is visible but low-contrast/tiny; M-38 is routed but not readable enough from the public crop. H-601 and H-1678 remain secondary-catalogue-only.

The directionality source queue now has a public-route triage layer, two reviewed no-overlay packet failures, one preflight-failed v2c packet, object-panel/signband preflight inventories, and a failed homogeneous v2f gate. `data/open_prototype/reports/effective_unicity_directionality_public_route_probe_summary.json` routes 79 top high-pressure rows through public CISI OCR and finds 38 public plate-route candidates, one data/register-only route, and 40 not found. `data/open_prototype/reports/effective_unicity_directionality_public_route_probe_v2_summary.json` preserves those counts while fixing the DjVu five-coordinate OCR parser bug. `data/open_prototype/reports/effective_unicity_directionality_route_visual_triage_summary.json` manually triages nine top route hits and marks four attempted target crops: `H-654`, `M-1310`, `M-1320`, and `M-811`. `data/open_prototype/reports/effective_unicity_directionality_blind_packet_review_summary.json` scores the underpowered v1 failure. `data/open_prototype/reports/effective_unicity_directionality_blind_packet_v2b_review_summary.json` scores the stricter v2b failure with 12 real negatives and three reviewers. `data/open_prototype/reports/effective_unicity_directionality_blind_packet_v2c_summary.json` records a v2c preflight failure: corrected OCR boxes and padded masks still leave page-context/catalogue leakage. `data/open_prototype/reports/effective_unicity_directionality_panel_crop_repair_v2_summary.json` creates 278 object-panel/signband crop candidates across 22 CISIs, but `docs/effective_unicity_directionality_visual_preflight_failures.md` records why the cleaned v2d shortlist is still not a packet: fixed denominator 9, reserve rows excluded, and mixed crop worlds. `data/open_prototype/reports/effective_unicity_directionality_signband_pool_v2e_summary.json` widens the crop-QA surface to 488 candidate crops across 38 public plate-route CISIs. `data/open_prototype/reports/effective_unicity_directionality_homogeneous_packet_v2f_preflight_summary.json` then rejects promotion: strict reuse lacks `M-811`, has seven original fixed real negatives, and has source-page collisions; derived top strips are acquisition-only. There is still no directionality blind review or claim promotion. This is acquisition and failure-discovery infrastructure only.

The directionality formula-family control is now in place at `docs/effective_unicity_directionality_formula_family_controls.md` and `data/open_prototype/reports/effective_unicity_directionality_formula_family_controls_summary.json`. It survives the strongest metadata-only source-convention collapse currently available, but records the L/R failure explicitly and increments no accepted counts.

The route-conditioned directionality support gate is now in place at `docs/effective_unicity_directionality_route_conditioned_control.md` and `data/open_prototype/reports/effective_unicity_directionality_route_conditioned_control_summary.json`. It rejects the tempting shortcut that public route availability itself is support: the 38 public-route rows score 0.684211, source-page collapse gives 0.625000, source-register collapse gives 0.619048, and matched route-label nulls reproduce or exceed the observed route subset score. Accepted counts remain zero.

The expanded Brahmi source-token gate is now in place at `docs/brahmi_source_token_descent_gate_v2.md` and `data/brahmi/source_token_brahmi_descent_v2_summary.json`. It scales the descendant-script attempt to 36 early Indoskript manuscripts, 1,342 Brahmi glyph features, 61 exact projection-gap source rows, and 611 source-token features. All 83 sign/orientation families fail the null gates, so accepted phonetic anchors remain zero.

The Brahmi independent source-token gate v3 is now in place at `docs/brahmi_independent_source_token_gate_v3.md` and `data/brahmi/brahmi_independent_source_token_gate_v3_summary.json`. It adds the stricter pre-review independence rule recommended by the skeptic: at least three unique token hashes, three unique CISIs, three unique source paths, unanimity after duplicate collapse, unchanged modal label, and shape/label null shares <= 0.01. All 83 families are blocked before review.

The BM `120573` object-level audit is now in place at `docs/bm120573_object_bridge_audit.md` and `data/meluhha/bm120573_object_bridge_audit_summary.json`. It checks the strongest Ur/Diqdiqqah cuneiform-on-Indus-style route against local Ur external rows and rejects the mapping. This prevents the route from being laundered into a sign-value anchor through the old `3898.1` same-site pattern temptation.

The Gadd Ur accession bridge audit is now in place at `docs/gadd_ur_accession_bridge_audit.md` and `data/meluhha/gadd_ur_accession_bridge_audit_summary.json`. It verifies two local row/object mappings, `3898.1/U17649` and `3899.1/U8685`, while rejecting the larger micro-bilingual hope: the modeled Gadd/BM/Penn surface has zero objects that combine an Indus sequence with readable cuneiform. This keeps the route as acquisition infrastructure, not a phonetic anchor.

The Gulf Type Indus external queue is now in place at `docs/gulf_type_indus_external_queue.md` and `data/meluhha/gulf_type_indus_external_queue.csv`. It converts Laursen Table 1 external `Gulf INDUS` rows `6-27` into a source-acquisition queue, with high-pressure next targets Kjaerum `279/319`, Mitchell 1986 figures `106-117`, Amiet `1643`, and Sarzec/Heuzey plate `30.3a-b`. It increments no accepted count.

The replacement branch Failaka/Kjaerum acquisition guardrail is now in place at `docs/failaka_kjaerum_acquisition_20260531.md` and `data/meluhha/failaka_kjaerum_acquisition_20260531_summary.json`. It keeps Laursen target rows `12` and `13` live while preventing a false promotion through adjacent CDLI publication `1773730`: the cached CDLI related artifacts cite catalogue spans/numbers `168-189`, `379`, `397`, and `399`, not `279` or `319`. The DAI route currently resolves only to anti-bot HTML. Aarhus University Press, CiNii Books, and Open Library routes are now cached as physical/bibliographic access routes, but none exposes the target catalogue pages digitally. Accepted external anchors remain zero.

The object-level onomastic value harness is now in place at `docs/object_level_onomastic_value_attempts.md` and `data/meluhha/object_level_onomastic_value_summary.json`. It is the direct value-attempt lane for future bridges: it records candidate assignments, forger runs, and skeptic reasons before any external-anchor claim can be promoted.

## Current Rule

No fluent translations. No phonetic values. No sign meanings. A structural claim increments `accepted_claim_counts.structural_findings` only after the ledger records both an acceptable forger false-positive rate and a hostile skeptic pass with no unresolved fatal objections.
