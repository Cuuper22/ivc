# 390-X context adversary

Date: 2026-05-31 America/Los_Angeles

Status: active adversarial gate, not goal completion.

## Question

Is the `002-390-X` branch-tail behavior really conditioned by the `002-390` frame, or is it just generic `390-X` behavior?

If `390-125` always continued everywhere, then the `002` part of the frame would become much less interesting. If `390-095` always closed everywhere, the same problem would hit the non-`125` side.

No value, phonetics, language identity, function, sign meaning, or translation is accepted.

## Outputs

- Runner: `data/open_prototype/tools/campaign_032_002_861_002390x_390_context_adversary_20260531.mjs`
- All `390-X` frames: `data/open_prototype/reports/campaign_032_002_861_002390x_390_context_adversary_20260531_frames390.csv`
- Branch summary: `data/open_prototype/reports/campaign_032_002_861_002390x_390_context_adversary_20260531_branch_summary.csv`
- Focus rows: `data/open_prototype/reports/campaign_032_002_861_002390x_390_context_adversary_20260531_focus_rows.csv`
- Decisions: `data/open_prototype/reports/campaign_032_002_861_002390x_390_context_adversary_20260531_decisions.csv`
- Summary: `data/open_prototype/reports/campaign_032_002_861_002390x_390_context_adversary_20260531_summary.json`

## Result

Status: `390_context_adversary_keeps_002_conditioning_live_no_values`.

The scan found `264` adjacent `390-X` frames in `metadata_filtered.csv`.

Focus branch behavior:

| Branch after `390` | All `390-X` rows | All terminal | All continuing | In `002-390` | In `002-390` terminal/continuing | Outside `002-390` terminal/continuing | Consequence |
|---|---:|---:|---:|---:|---|---|---|
| `125` | 9 | 3 | 6 | 4 | `0 / 4` | `3 / 2` | `390-125` alone does not explain continuation; `002` conditioning stays live. |
| `095` | 3 | 2 | 1 | 2 | `2 / 0` | `0 / 1` | Closure after `002-390-095` is not generic `390-095`; sample tiny. |
| `692` | 1 | 1 | 0 | 1 | `1 / 0` | `0 / 0` | No outside-`002` control. |
| `705` | 2 | 2 | 0 | 2 | `2 / 0` | `0 / 0` | No outside-`002` control; source binding still decisive. |
| `530` | 1 | 0 | 1 | 1 | `0 / 1` | `0 / 0` | H-773 remains singleton/panel-bound pressure. |
| `590` | 73 | 13 | 60 | 1 | `0 / 1` | `13 / 59` | `3335.1` stays formula-family pressured. |

## Decision

The adversary does not kill the `002-390-X` object.

What it blocks:

- It blocks a lazy claim that `390-125` itself is always a continuing pair. Outside `002-390`, `390-125` is terminal in 3 rows and continuing in 2.
- It blocks a lazy claim that `390-095` itself is always terminal. The one non-`002` `390-095` row continues.
- It reinforces formula-family pressure for `390-590`, because `390-590` is broad and mostly continuing outside the `002-390` lane.

What remains live:

- `002` before `390` remains a plausible conditioning clue for the `125` continuation pattern.
- `095` closure inside `002-390` remains interesting, but H-1993 is still route-only and the sample is tiny.
- `692` and `705` cannot be tested outside `002-390` in this scan because they occur only in the `002-390` frame.

This is structural pressure only. It does not promote grammar/function, because the strict source layer is still too thin and unmatched by predecessor.

Accepted value, phonetics, language identity, function, sign meaning, and translation remain 0.
