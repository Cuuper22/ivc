# M-45 double-grid-pair segmentation gate

Date: 2026-07-12 America/Los_Angeles

Decision: `SOURCE_VISIBLE_SEVEN_UNITS_THIRD_617_DOUBLE_GRID_CONFIRMATION`.

## Question

M-45 is the third direct test of the recurring Lipi `617` versus Mayig `P268 P268` discrepancy. Lipi stores six tokens; Mayig stores seven graphemes. The live question was whether the source again preserves two bounded grids or breaks the emerging pair policy.

## Direct evidence

- Lipi row `2573.1`: `+617-831-013-302-840-032+`, text length 6, signs 6, direction `R/L`.
- Mayig `M-45A`: `P268 P268 P372 P139 P205 P349 P145`, grapheme count 7.
- CISI 1 PDF page 58 / printed page 22 publishes `M-45 A` and `M-45 a`.
- Both views show seven bounded inscription units. The initial reading-order pair consists of two grid forms with separate outer boundaries.

The Mayig pair is source-supported. M-45 has seven structural units, not six.

## Object-level decision

- Use source-unit count `7` for `M-45`.
- Preserve both grids as separate positions.
- Reject Lipi `617` as a literal one-unit structural count on this object.
- Normalize the source-confirmed prefix as `<GRID>, <GRID>, ...`; only the second grid is adjacent to the following source unit.
- Do not rewrite either raw corpus or infer a sign value from the grid forms.

## Research consequence

M-45 joins M-161 and M-25 as the third independent source-confirmed instance in which one Lipi `617` corresponds to two separately bounded source grids and Mayig `P268 P268`. No checked object contradicts the pattern. The full overlap cluster contains five exact cases: `M-161`, `M-25`, `M-45`, `M-81`, and `M-4`.

For the three checked objects, the former `compound_or_omission` uncertainty no longer affects structural normalization: each must be expanded to two source positions regardless of whether Lipi's internal convention is called a compound token or repeat suppression. Across all five overlap rows, `617 -> P268 P268` is now a strong catalogue-expansion candidate, not yet a full-corpus rule. M-81 is the next test because it places `617` inside the sequence rather than initially.

This gate does not establish a sign value, meaning, phonetic reading, language identification, translation, or decipherment claim.

## Preserved evidence

- `research/data/sign_crosswalk/source_panels/m045_double_grid_pair/M-45_A_a_CISI1_pdf58_print22.png`, SHA-256 `3B139296BE1B4DD8285DB1E48394556C2AB541C60DED8751BA4843FAA4365156`.
- CISI 1 PDF SHA-256: `47A4DA4227CFB7BAA56D6A561E25797225499D0EC54F5A702A82C67AB4746D4D`.
- Mayig `m045.json`: `8111D98C9E2023053054901CA10F9A0B9120791999B9197F974B76133CA1E7D1`.
- Mayig `P268.json`: `34248A590A6E9508C42747CDA83871240599456F6BFE78D8E98EEB5B11E3F139`.
