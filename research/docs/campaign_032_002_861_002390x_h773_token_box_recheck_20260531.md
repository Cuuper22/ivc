# H-773 token-box recheck for `002-390-X`

Date: 2026-05-31 America/Los_Angeles

Status: active source/sign gate, not goal completion.

## Target

H-773 is the non-`125` continuation exception inside the live `002-390-X` frame:

- Local row: `1665.1`
- Object: `H-773`
- Site/type: Harappa `TAB:B`
- Material/shape: faience, plano-convex
- Local text: `+740-798-803-002-390-530-741+`
- Local direction: `R/L`
- Structural stressor: `002-390-530-741`, where `530` continues to `741`

The question is not whether H-773 exists in metadata. It does. The question is whether the source panel, on its own, supports the exact continuation strongly enough to count as a strict anti-`125` exception.

## Source acquired

Archive.org CISI Pakistan page `n358` was downloaded:

- Full page: `tmp/source_route_recheck_20260531/cisi_pakistan_n358_w2400.jpg`
- H-773 A tight crop: `tmp/source_route_recheck_20260531/cisi_pakistan_n358_h773_A_signband_tight_w2400.jpg`
- Token overlay: `tmp/source_route_recheck_20260531/cisi_pakistan_n358_h773_A_token_box_overlay_caption_w2400.jpg`

The page visibly contains H-773 A/B. H-773 A has seven visible units and is the plausible side for local row `1665.1`.

Additional acquisition check: Archive.org `n358_w4000` and `n358_w6000` returned the same byte payload as the stored `n358_w2400` image, so they did not improve the crop. A direct attempt to retrieve `Pakistan_0358.jp2` from the IA JP2 zip route returned HTTP `503`. No higher-quality image was earned in this pass.

## Token-box result

Under the local `R/L` reading policy, the physical left-to-right order is treated as the reverse of the local sequence:

`741 | 530 | 390 | 002 | 803 | 798 | 740`

The target local continuation `002-390-530-741` is therefore the left physical block in reverse reading order:

`002 <- 390 <- 530 <- 741`

Approximate boxed windows on the tight crop:

| Local sign | Physical x-window | Role |
|---|---:|---|
| `741` | 0-75 | terminal after `530`, physically left edge |
| `530` | 75-225 | non-`125` branch after `390` |
| `390` | 225-340 | branch-head frame sign |
| `002` | 340-465 | frame opener |
| `803` | 465-565 | pre-frame sign in local reading order |
| `798` | 565-695 | earlier sign in local reading order |
| `740` | 695-815 | earlier sign in local reading order |

## Decision

H-773 upgrades from metadata-only to source-panel-bound pressure.

It does not upgrade to strict token evidence. The source crop is compatible with a seven-unit H-773 A row and with the boxed `002-390-530-741` window under the local `R/L` policy, but the exact sign identities and the `530 -> 741` boundary are still catalog-mediated. The left edge and `530/390` region are not clean enough to independently prove the numeric sequence from the image alone.

Current status:

`source_panel_acquired_boxed_window_compatible_token_not_strict`

Addendum, 2026-05-31:

Kenoyer/Meadow 1997 adds a side-label guard, not a target upgrade. The text route maps `p.57:5 / Plate XCIV item 351 / object 12377 / Square I 14/22` to `H-773B`, with a note that it is "one side only" of the compared tablet set. The local source pointer `12377351` likely reflects object `12377` plus plate item `351`, and Vats Plate XCIV item `351` is visible in the local plate image. But this route names side `B`; the target `002-390-530-741` row is local side `A` (`1665.1`). Local side `B` is `1665.2 +700-033-711-335-575-413+`.

Addendum status:

`h773_vats_12377_351_side_b_bridge_no_target_a_upgrade`

Artifact: `docs/campaign_032_002_861_002390x_h773_side_guard_recheck_20260531.md`

Report: `data/open_prototype/reports/campaign_032_002_861_002390x_h773_side_guard_recheck_20260531.csv`

Crop: `tmp/002390x_h773_vats_side_guard_20260531/Vats_Plate_XCIV_351_candidate_H773B_tight_from_v2.png`

## Consequence

The adversarial model gains pressure, because H-773 is no longer a ghost metadata row — there is a real panel behind it. But the positive model does not have to absorb a strict source-bound non-`125` continuation yet.

Use H-773 as:

- Yes: panel-bound pressure against a lazy "`125` is the only continuation branch" claim.
- No: strict anti-`125` token evidence.
- No: sign value, phonetics, function, language identity, meaning, or translation.

Next gate: either obtain a cleaner H-773 image/sign list that independently fixes `530` and `741`, or keep H-773 in the boxed-compatible-but-not-strict tier while moving source effort to H-1993 and Dholavira `4237.1`.

Addendum, Lipi image-map guard:

The cloned Yajnadevam/Lipi repository contains `H-773_a.jpg` and `H-773_b.jpg` in `public/seal_images`, mapped by `src/assets/data/seal_id_and_image_mapping.json`. This does not upgrade H-773. `H-773_a.jpg` is a low-resolution derivative (`269x170`), weaker than the existing CISI source-page crop (`820x260`), and it still does not independently prove `530`, `741`, or the continuation boundary.

Artifact: `docs/campaign_032_002_861_002390x_h773_lipi_image_map_guard_20260531.md`

Report: `data/open_prototype/reports/campaign_032_002_861_002390x_h773_lipi_image_map_guard_20260531.csv`
