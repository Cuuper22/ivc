# H-2148 `110` / Parpola Sign 41 Crosswalk Decision Gate

Date: 2026-05-26

## Question

What would make local token `110` and Parpola sign no. 41 an admissible crosswalk rather than a visual hunch?

## Current State

- H-2148 is the cleanest source-visible/count-mapped branch target through Kenoyer 2005 Figure 14.1.
- H-940 is source-visible as local `+110+` but visually split at current public quality.
- H-2147 is public object-visible but only component-level pressure after raw recropping.
- H-2100, H-2152, and Kanmer `4881.1` are mandatory outside controls and remain source-gated.

## Admissible Crosswalk Criteria

Local `110` <-> Parpola sign no. 41 becomes admissible only if all of these hold:

1. H-2148's source image clearly shows the clean single-panel target.
2. The same physical side/panel is explicitly mapped to the local `+110+` row or equivalent local token row.
3. Parpola's sign no. 41 assignment is demonstrably for that same physical side/panel, not the companion side.
4. The graphic form is stable under source-quality inspection: a single compound branching figure, not paired verticals, damage, hatching, or a multi-sign cluster.
5. At least one outside control or independent branch witness supports the same local-token/Parpola-sign relation without relying on H-2148 alone.
6. H-940 either remains excluded as a distinct graphic class or rejoins only after source-grade imagery shows the same topology.

## Kill Criteria

The crosswalk should be rejected if any of these occur:

1. H-2148's clean panel maps to a local row other than `110`.
2. Parpola sign no. 41 is shown to refer to the opposite or companion side.
3. Source-grade H-2148 evidence shows the supposed target is damage, hatching, or a multi-sign cluster rather than one graphemic unit.
4. H-2100, H-2152, or Kanmer controls show the same graphic form explicitly mapped to a different local token.
5. H-940 source-grade imagery confirms a different topology but still belongs to Parpola's no. 41 grouping, implying Parpola no. 41 is broader than local `110`.
6. Local `110` rows split into multiple source-visible graphic classes with no defensible allographic rule.

## Keep-P0 Conditions

Keep the crosswalk as a P0 hypothesis if:

- H-2148 remains visually clean but side-to-local-row assignment is absent.
- H-2147 stays visually suggestive but fragmentary or unmapped.
- H-940 stays source-visible but visually split.
- Outside controls become visible but still lack explicit local-token alignment.
- Parpola grouping remains text-level prior pressure without panel-level source proof.
- The branch has only one clean target and no independent mapped recurrence.

## Immediate Test

Can H-2148's clean single-panel source image be explicitly tied to both local token `110` and Parpola sign no. 41 on the same physical side?

Until yes: P0, source-targeted only. No sign value, phonetic value, language identity, or translation follows.
